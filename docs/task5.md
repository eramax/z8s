currently this z8s has a big problem, i always get multiple pid running. z8s is supposed to be the only pid running and it can spwan some nodes - i dont know if we need to make each node as a process or not but it will be good. anyway always when multiple pids running any kubectl command get hang forever. I want to do a real solution. remember z8s is like k3s , it can start or stop nodes, and it can make nodes join it. we have a real deadlock which i dont know where. and our async and system is not optimized or clean. your goal is to clean it and optimize it and make z8s can run (with sudo) and i can start or stop multiple nodes and no issues. and it prevent from having two z8s running on the same machine. also for the files it creates or uses some are node related and some are z8s related we need to distingue. investigate well and fix all the issues now. please try to avoid hardcoded or stupid code. make only clean code. also make sure always when u stop a node or a resource to do cleaning up. 