you are a professional rust developer with creative thinking and linux expert and professional in utilizeing design patterens and writing less code to fix and achive complex tasks. please read the /home/abb/dev/z8s/README.md and /home/abb/dev/z8s/log.md and the current error [Pasted text #1 +18 lines]. Now i want to know if you have better ideas how we can refactor and improve our code. I think the builder design patteren is the best. also we need to stick with k8s-openapi structs. also i dont know if we still need serde and its components. also we were thinking what approach should we use to handle next phases. should we be rootless as we are now or use rootlesskit or we need to improve our approach to be perfect to achive all our goals.remember this will be the init process for some systems (new os based on linux) and it should be strong to supervise and control the processes / services. also we need to avoid any escalpe issues also some pods will need to use root to install apps inside. also we need to be not limited to support running docker images and normal linux programes and daemons all based on yaml files compitable with k8s and we need off course to support deployments and scalling including exec and interactive shell and logs and describe. we need to build our own cni which might utlize ebpf or not (not sure know how its complexity but it is prefered if the kernel supports.) also networking and network policies and namespaces. and storage (volumes and configmaps and secrets ) and env. and readness and liveness. with strong (force limits e.g, cpu and ram  and storage for each pod). we have to support restart policy and support labels and we need to include good  and fast ingress controller (maybe we build ours or use something very lightweight) our app should be small and lightweight and can set idle if not called. maybe we need to install uidmap package, or other packages to help us. for the current containers they should be really isolated and running reboot or so never affect the host. we plan to have support for other nodes to join (maybe the nodes on the same host or on another machine remotly ). we made a great poc now and want to move to the next step and make something we proude off. when i started this  project I wanted something like s6 with its components and k3s and linux cluster and linux capability systesms. our daemans should be very lightweight on both cpu and ram and works async.